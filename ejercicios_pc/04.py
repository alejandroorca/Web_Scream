# 04. Crea una función que reciba un parámetro de entrada de tipo array, o lista, de tipo numérico y que devuelva un array, o lista, con la media y con otro array, o lista, con los números ordenados de mayor a menor. Ejecuta 3 llamadas de ejemplo de la función creada.

lista = [1, 5, 9]

def listaa(lista[0],lista[2]):
	lista_orden = []
	lista_orden.append(lista[0])
	lista_orden.append(lista[2])

	a = sorted(lista_orden)
    
    conv = int(lista[0])
    conv1 = int(lista[2])

     lista_media = []

     media = conv / 2
     media1 = conv1 / 2

     lista_media.append(media)
     lista_media.append(media1)
     return lista

     print(lista(lista[1], 45))
	
